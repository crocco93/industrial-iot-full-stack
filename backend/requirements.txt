# FastAPI and ASGI server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database
pymongo==4.6.0
motor==3.3.2
beanie==1.26.0

# WebSocket support
websockets==12.0

# Authentication and security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.6
PyJWT==2.8.0  # ✅ ADDED for JWT tokens
bcrypt==4.1.2  # ✅ ADDED for password hashing

# Environment and configuration
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Redis for caching and sessions
redis==5.0.1
aioredis==2.0.1

# HTTP client for integrations (N8N, Ollama)
httpx==0.25.2
aiohttp==3.9.1

# System monitoring and health checks
psutil==5.9.6

# Cryptography for security
cryptography==41.0.7

# Utilities
typing-extensions==4.8.0

# Logging
loguru==0.7.2

# Development and testing
requests==2.31.0
pytest==7.4.3
pytest-asyncio==0.21.1

# Date/time utilities
python-dateutil==2.8.2  # ✅ ADDED for date handling

# ===== INDUSTRIAL PROTOCOL LIBRARIES =====

# Modbus TCP/RTU support
modbus-tk==1.1.3
pymodbus==3.5.2

# OPC-UA support
asyncua==1.0.4

# MQTT support
paho-mqtt==1.6.1
aiomqtt==1.2.1  # ✅ ADDED for async MQTT export

# EtherNet/IP support (Allen-Bradley PLCs)
pycomm3==1.2.14
cpppo==5.2.5

# BACnet support
bacpypes==0.18.7

# CANopen support
canopen==2.2.0
python-can==4.3.1

# Profinet/Siemens S7 support
snap7-python==1.3.0  # ✅ ADDED for Siemens S7 PLC communication

# Additional networking utilities
scapy==2.5.0
netifaces==0.11.0

# Serial communication (for RTU protocols)
pyserial==3.5

# XML parsing (for some industrial protocols)
lxml==4.9.3

# Industrial data format support
bitstring==4.1.2

# Data validation and serialization
marshmallow==3.20.2  # ✅ ADDED for advanced data validation

# Performance monitoring
prometheus-client==0.19.0  # ✅ ADDED for metrics export